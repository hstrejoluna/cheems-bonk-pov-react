import * as React from "react";

const Header = () => {
  return (
    <header className="header-cheems">
        <button>About</button>

      <h1>Cheems Call</h1>
      <button>BONK!</button>
    </header>
  );
};

export default Header;
